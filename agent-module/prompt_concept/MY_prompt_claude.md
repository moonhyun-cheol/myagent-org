# DEPRECATED — use MY_prompt.md + product data API

This file no longer ships embedded catalog or internal strategy data.

For MY Agent: `prompt_concept/MY_prompt.md` via `brand_concept` skill.  
For product facts: `product_data_base_url` or slash `/childasin`, `/모델가계도`.

Do not use this variant for new sessions.
