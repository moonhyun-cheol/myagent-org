# Pack knowledge snapshot

Live brand philosophy comes from:

`http://192.168.1.248:8080/api/brand-manual/current.md`

This folder is the **offline SKU/spec snapshot** the RA uses for model matching, `.dev` matrices, and feasibility. It is not the live manual.

Keep: strategy v3.1, lineup overlay, spec engines, model row index, visual/loadout refs.

Do not put here: `options.db`, Excel, `.chroma`, NAS extracts, old strategy v2.x.
